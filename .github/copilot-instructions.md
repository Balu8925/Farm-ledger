# AI Coding Agent Instructions for Farm Project

## Project Overview
This is a .NET project built using MSBuild. The project appears to be in early development stages with no source code files currently present.

## Build System
- **Build Command**: Use the VS Code task "build" or run `msbuild /property:GenerateFullPaths=true /t:build /consoleloggerparameters:NoSummary`
- **Build Tool**: MSBuild (Microsoft Build Engine)
- **Platform**: Windows (based on terminal and build system)

## Development Environment
- **IDE**: Visual Studio Code
- **Terminal**: PowerShell
- **OS**: Windows

## Key Patterns and Conventions
*No specific patterns identified yet - codebase is empty*

## File Structure
*Project structure not yet established*

## Dependencies
*No dependencies configured yet*

## Workflows
- Build the project using the "build" task in VS Code
- Add source files to the workspace root or appropriate subdirectories
- Configure project files (.csproj, .sln) as needed

## Notes
This instructions file will be updated as the codebase grows. Currently, the project is a blank .NET solution.